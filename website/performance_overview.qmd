# Performance overview

## Performance results

The initial example benchmark shows sub-linear scaling across the current 2- and 6-reaction example aggregations: the larger CellRanger input increases estimated wall time by only ~1.4-fold. In practice, this means that larger projects should benefit substantially from the pipeline's reaction-level and branch-level parallelism when sufficient compute resources are available, rather than scaling like a fully serial workflow.

![Estimated wall time to each aggregation's final `multimodal_Seurat_object` target by total CellRanger input nuclei. Stacked bar segments show the largest critical-path target-step contributions, remaining steps are collapsed into `Other`, the line connects total critical-path wall time, and point labels show the number of reactions in each aggregation. Estimates are generated from recorded `targets` runtime metadata with `cache_multimodal_seurat_walltime()`, using the DAG critical path and treating dynamic branches as concurrently runnable. This interim figure will be replaced with the full 5-, 10-, 20-, and 40-reaction benchmark once those aggregations have been generated.](../outputs/benchmark/multimodal_seurat_walltime.png){#fig-multimodal-seurat-walltime}
